{
  "name": "country-beach-escape",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name country-beach-escape"
  },
  "devDependencies": {
    "vite": "latest",
    "wrangler": "latest"
  }
}
